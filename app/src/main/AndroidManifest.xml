<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rishabh.matchgame">

    <application
        android:largeHeap="true"
        android:hardwareAccelerated="false"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenLayout"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OptionFragment" />
        <activity android:name=".GameActivity" />
        <activity
            android:name=".LearnList"
            android:label="@string/learn_name"
            android:theme="@style/LearnTheme" />

        <activity android:name=".PlayList"
            android:label="@string/play_name"
            android:theme="@style/PlayTheme">
        </activity>
        <activity android:name="learn.Learn_Activity"
            android:label="Learn Through Images"></activity>
    </application>

</manifest>